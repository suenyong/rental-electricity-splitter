import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [{ url: "https://rental-electricity-splitter-tw.su-yu-grace.chatgpt.site", changeFrequency: "monthly", priority: 1 }];
}
